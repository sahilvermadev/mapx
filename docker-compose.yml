# docker-compose.yml
version: '3.8' # You can remove this line if you want, the warning will go away

services:
  db:
    image: vdauchy/postgis-vector:latest
    container_name: mapx_db_container
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    container_name: mapx_backend_container
    command: npm run dev # Assuming you have a 'dev' script in backend/package.json
    volumes:
      - ./backend:/app # Mount host code for hot-reloading
      # - /app/node_modules # Prevent host's node_modules from overwriting container's
    ports:
      - "5000:5000"
    environment:
      DATABASE_URL: postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}
      JWT_SECRET: ${JWT_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      SESSION_SECRET: ${SESSION_SECRET}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      GROQ_API_KEY: ${GROQ_API_KEY}
      # Add other backend specific env vars here
    depends_on:
      db:
        condition: service_healthy

volumes:
  db_data: